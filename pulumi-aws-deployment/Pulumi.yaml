name: pulumi-aws-deployment
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: pulumi deployment for aws for frontend and backend of learn colemak
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
