
const RestartButton = ({handleRestart, className}) => {
    
        return <button onClick={handleRestart} className={className}>
            Restart
            </button>
    }

export default RestartButton;